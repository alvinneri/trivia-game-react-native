import { Platform } from "react-native";
import { colors } from "react-native-elements";

export const theme = {
  colors: {
    background: "#86b9ce",
    info: "#01016f",
    danger: "#d8031c",
  },
};
